<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main"
    tools:context=".MainActivity">

    <View
        android:id="@+id/v_main_bg"
        android:layout_width="370dp"
        android:layout_height="80dp"
        android:background="#000000"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="20dp"
        />

    <TextView
        android:id="@+id/tv_main_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="시작하기"
        android:textAppearance="@style/Head2_M_22"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@id/v_main_bg"
        app:layout_constraintEnd_toEndOf="@id/v_main_bg"
        app:layout_constraintStart_toStartOf="@id/v_main_bg"
        app:layout_constraintTop_toTopOf="@id/v_main_bg" />

</androidx.constraintlayout.widget.ConstraintLayout>